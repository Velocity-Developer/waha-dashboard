const qrModalEl = document.getElementById('qrModal');
const qrBodyEl = document.getElementById('qrBody');
const qrModal = qrModalEl ? new bootstrap.Modal(qrModalEl) : null;
let qrPollTimer = null;
let qrCountdownTimer = null;
let qrActiveName = '';
let qrCountdown = 0;
const CONNECT_SOUND = new Audio('data:audio/wav;base64,UklGRlYAAABXQVZFZm10IBAAAAABAAEAQB8AAEAfAAABAAgAZGF0YTIAAAAAAAcPEBAPBwAA+Pj4AAcPEA8PBwAA+Pj4AAcPEA8PBwAA+Pj4AAcPEA8PBwAA+Pj4AA==');
function showToast(message, category='info'){const c=document.querySelector('.toast-container')||(()=>{const e=document.createElement('div');e.className='toast-container position-fixed top-0 end-0 p-3';document.body.appendChild(e);return e})();const d=document.createElement('div');d.className=`toast align-items-center text-bg-${category==='danger'?'danger':category==='success'?'success':category==='warning'?'warning':'info'} border-0 show`;d.innerHTML=`<div class="d-flex"><div class="toast-body">${message}</div><button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast"></button></div>`;c.appendChild(d);setTimeout(()=>d.remove(),5000)}
function stopQrPolling(){if(qrPollTimer)clearTimeout(qrPollTimer);if(qrCountdownTimer)clearInterval(qrCountdownTimer);qrPollTimer=null;qrCountdownTimer=null}
function setQrBody(html){if(qrBodyEl)qrBodyEl.innerHTML=html}
function escapeHtml(value){return String(value).replace(/[&<>\"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}
function updateCountdownLabel(){const el=document.getElementById('qrCountdown');if(el)el.textContent=`refresh lagi ${qrCountdown} detik`}
function startCountdown(seconds=3){if(qrCountdownTimer)clearInterval(qrCountdownTimer);qrCountdown=seconds;updateCountdownLabel();qrCountdownTimer=setInterval(()=>{qrCountdown=Math.max(qrCountdown-1,0);updateCountdownLabel()},1000)}
function copyPairingCode(code){navigator.clipboard.writeText(code).then(()=>showToast('Pairing code berhasil disalin','success')).catch(()=>showToast('Gagal salin pairing code','danger'))}
function copyToken(id){const el=document.getElementById(id);if(!el)return;navigator.clipboard.writeText(el.value).then(()=>showToast('Token berhasil disalin','success')).catch(()=>showToast('Gagal salin token','danger'))}
function toggleToken(id,btn){const el=document.getElementById(id);if(!el)return;const nextType=el.type==='password'?'text':'password';el.type=nextType;if(btn){const show=nextType==='text';btn.innerHTML=show?'<i class="bi bi-eye-slash"></i>':'<i class="bi bi-eye"></i>';btn.title=show?'Hide token':'Preview token';btn.setAttribute('aria-label',show?'Hide token':'Preview token')}}
function playConnectedFx(){try{CONNECT_SOUND.currentTime=0;CONNECT_SOUND.play().catch(()=>{})}catch(e){}}
async function loadQr(name,firstLoad=false){qrActiveName=name;if(firstLoad)setQrBody('<div class="spinner-border"></div><p class="mt-2">Loading QR...</p>');try{const r=await fetch(APP_ROOT+'/session/qr/'+encodeURIComponent(name),{cache:'no-store'});const d=await r.json();if(d.qr){setQrBody('<img src="'+d.qr+'" class="img-fluid" style="max-width:250px"><p class="mt-2 text-light-emphasis">Scan dengan WhatsApp</p><p class="mt-1 text-light-emphasis"><small id="qrCountdown">refresh lagi 3 detik</small></p>')}else if(d.code){const safeCode=escapeHtml(d.code);setQrBody('<h2 class="fw-bold" style="letter-spacing:8px">'+safeCode+'</h2><p class="mt-2 text-light-emphasis">Masukkan kode di WhatsApp</p><div class="d-flex justify-content-center mt-2"><button type="button" class="btn btn-outline-primary btn-sm" id="copyPairCode"><i class="bi bi-copy"></i> Copy pairing code</button></div><p class="mt-2 text-light-emphasis"><small id="qrCountdown">refresh lagi 3 detik</small></p>');const copyBtn=document.getElementById('copyPairCode');if(copyBtn)copyBtn.addEventListener('click',()=>copyPairingCode(d.code),{once:true})}else{setQrBody('<p class="text-warning">QR belum tersedia</p><p class="mt-1 text-light-emphasis"><small id="qrCountdown">refresh lagi 3 detik</small></p>')}}catch(e){setQrBody('<p class="text-danger">Gagal load QR</p><p class="mt-1 text-light-emphasis"><small id="qrCountdown">refresh lagi 3 detik</small></p>')}if(!document.body.classList.contains('modal-open')||qrActiveName!==name)return;try{const rs=await fetch(APP_ROOT+'/session/status/'+encodeURIComponent(name),{cache:'no-store'});const s=await rs.json();if(s.status==='WORKING'){stopQrPolling();playConnectedFx();showToast(`Session ${name} connected`,'success');setQrBody('<div class="text-center"><i class="bi bi-check-circle" style="font-size:48px;color:var(--success)"></i><p class="mt-2 fw-bold">Session connected</p><p class="text-light-emphasis">Halaman akan reload...</p></div>');setTimeout(()=>window.location.href=APP_ROOT+'/',1200);return}}catch(e){}stopQrPolling();startCountdown(3);qrPollTimer=setTimeout(()=>loadQr(name),3000)}
document.querySelectorAll('.qr-btn').forEach(btn=>btn.addEventListener('click',()=>{if(!qrModal)return;qrModal.show();loadQr(btn.dataset.name,true)}));if(qrModalEl)qrModalEl.addEventListener('hidden.bs.modal',()=>{stopQrPolling();qrActiveName='';setQrBody('<div class="spinner-border"></div><p class="mt-2">Loading QR...</p>')});if(typeof AUTO_QR!=='undefined'&&AUTO_QR){const btn=document.querySelector(`.qr-btn[data-name="${AUTO_QR}"]`);if(btn)setTimeout(()=>btn.click(),250)}
document.querySelectorAll('.edit-user-btn').forEach(btn=>btn.addEventListener('click',()=>{const a=id=>document.getElementById(id);if(a('edit_user_id'))a('edit_user_id').value=btn.dataset.id;if(a('edit_username'))a('edit_username').value=btn.dataset.username;if(a('edit_role'))a('edit_role').value=btn.dataset.role;const pwd=document.querySelector('#editUserModal .password-input');if(pwd)pwd.value=''}));
document.querySelectorAll('.toggle-password').forEach(btn=>btn.addEventListener('click',()=>{const input=btn.closest('.input-group').querySelector('.password-input');const icon=btn.querySelector('i');const show=input.type==='password';input.type=show?'text':'password';icon.className=show?'bi bi-eye-slash':'bi bi-eye'}));
const confirmModalEl=document.getElementById('confirmModal');
const confirmMessageEl=document.getElementById('confirmMessage');
const confirmSubmitBtn=document.getElementById('confirmSubmitBtn');
const confirmModal=confirmModalEl?new bootstrap.Modal(confirmModalEl):null;
let confirmTargetForm=null;
document.querySelectorAll('form.js-confirm').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();confirmTargetForm=form;if(confirmMessageEl)confirmMessageEl.textContent=form.dataset.confirm||'Lanjut?';if(confirmModal)confirmModal.show();else form.submit()}));
if(confirmSubmitBtn)confirmSubmitBtn.addEventListener('click',()=>{if(confirmModal)confirmModal.hide();if(confirmTargetForm){const form=confirmTargetForm;confirmTargetForm=null;form.submit();}});
